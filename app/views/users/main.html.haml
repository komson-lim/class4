%p{:id => "notice"}= notice 
= form_with() do |form|
    .field
        = form.label :email
        = form.text_field :email
    .field
        = form.label :password
        = form.password_field :password
    .actions 
        = form.submit "login"
       
       
